function bubbleSort(arr: integer[], size: integer) => void 
{
  localvar n    : integer;
  localvar i    : integer;
  localvar j    : integer;
  localvar temp : integer; 
    while (i < n-1) {
    while (j < n-i-1) {
      if (arr[j] > arr[j+1]) 
        then {
          n = 1 + 2;
        } else ;
      };
  };
}